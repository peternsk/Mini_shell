#include "minishell.h"

bool	ft_is_char_alpha(int c)
{
	if ((c >= 65 && c <= 90) || (c >= 97 && c <= 122))
		return (true);
	return (false);
}

bool	valid_key_name(char *key)
{
	int i;

	i = 0;
	while (key[i] != '\0')
	{
        if(ft)
	}
}
